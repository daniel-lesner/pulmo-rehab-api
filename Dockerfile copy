FROM ruby:3.3.5

WORKDIR /app

RUN apt-get update -qq && apt-get install -y nodejs postgresql-client git build-essential && gem install bundler

COPY Gemfile Gemfile.lock ./
RUN bundle install
COPY . .

CMD ["bash", "-c", "rm -f tmp/pids/server.pid && bundle exec rails s -b 0.0.0.0"]